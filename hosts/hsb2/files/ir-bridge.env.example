# IR → Sony TV Bridge Configuration
# Location: /etc/ir-bridge.env
# Permissions: chmod 600 (only root readable)

################################################################################
# Sony TV Configuration (Required)
################################################################################

# Sony Bravia TV IP address
SONY_TV_IP=192.168.1.137

# Pre-Shared Key (PSK) for TV authentication
# Get from TV: Settings → Network → Home Network Setup → IP Control → Pre-Shared Key
# Replace with actual PSK from ~/secrets/sonytv.env
SONY_TV_PSK=YOUR_PSK_HERE

################################################################################
# FLIRC Device Configuration
################################################################################

# Input device path for FLIRC USB receiver
# Usually /dev/input/event0 on Raspberry Pi Zero W
FLIRC_DEVICE=/dev/input/event0

################################################################################
# MQTT Configuration (for debug/status reporting)
################################################################################

# MQTT broker host (usually hsb1)
MQTT_BROKER=192.168.1.101

# MQTT broker port
MQTT_PORT=1883

# MQTT credentials (if required)
# Leave empty for anonymous access
MQTT_USER=
MQTT_PASS=

# Base MQTT topic for publishing
MQTT_TOPIC=home/hsb2/ir-bridge

################################################################################
# Behavior Configuration
################################################################################

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Debounce time in milliseconds (prevents double commands)
# Increase if you experience double commands
DEBOUNCE_MS=300

# HTTP retry configuration
RETRY_COUNT=3
RETRY_DELAY=1.0
