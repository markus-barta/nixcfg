.idea
./configuration.nix
./hardware-configuration.nix
/configuration.nix
/hardware-configuration.nix
/*.qcow2
result
.shared/*
!.shared/common.just

# macOS
.DS_Store

# Devenv
.devenv*
devenv.local.nix

# direnv
.direnv
.shared

# pre-commit
.pre-commit-config.yaml

# Private network configuration (contains MAC addresses, device info)
# Plain text version - kept locally only
**/static-leases.nix
# Encrypted version stored in secrets/ directory with agenix

# DNS management (manual Cloudflare export backups)
# Keep declarative Terraform/OpenTofu configs in git
# But exclude exported zone files and sensitive API keys
dns-backup-*.json
cloudflare-*.json
.terraform/
*.tfstate
*.tfstate.backup

# Secrets inventory (temporary working document)
# Contains references to secrets during migration
docs/secrets-inventory.md

# Deployment status and TODO files
hosts/miniserver99/real-static-leases.nix

# Security-sensitive investigation and documentation
# Everything under docs/private/ is gitignored (investigations, migrations, sensitive docs)
docs/private/
*-private/
*.sensitive.*

# Host-specific secrets and access documentation (per-host emergency access info)
hosts/*/secrets/
# But allow encrypted versions to be committed
!hosts/*/secrets/*.age

# Infrastructure secrets (same pattern as hosts)
infrastructure/*/secrets/
# But allow encrypted versions to be committed
!infrastructure/*/secrets/*.age

# Patrizio's archived hosts (reference only)
hosts/archive/

# Network topology with MAC addresses (keep local only)
hosts/hsb8/examples/network-reference-pihole-backup.md

# Legacy/Manual Docker Secrets (Safety Guard)
id_rsa
id_rsa.pub
*.env
!*.example.env
