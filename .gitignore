.idea
./configuration.nix
./hardware-configuration.nix
/configuration.nix
/hardware-configuration.nix
/*.qcow2
result
.shared

# Devenv
.devenv*
devenv.local.nix

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# Private network configuration (contains MAC addresses, device info)
# Plain text version - kept locally only
**/static-leases.nix
# Encrypted version stored in secrets/ directory with agenix
