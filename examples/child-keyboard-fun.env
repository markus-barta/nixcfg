# Child Keyboard Fun Configuration
# Copy to /etc/child-keyboard-fun.env and customize

# Device path (find with: cat /proc/bus/input/devices | grep -A 10 'ACME')
KEYBOARD_DEVICE=/dev/input/event0

# Sound directory (place .wav or .mp3 files here)
SOUND_DIR=/var/lib/child-keyboard-sounds

# Per-key sound mappings (optional)
# Format: KEY_<name>=sound:<filename> or KEY_<name>=random

# Examples:
# KEY_A=sound:letter_a.wav
# KEY_B=sound:letter_b.mp3
# KEY_O=sound:oooooooo.wav
# KEY_SPACE=random

# Default behavior: unmapped keys play random sound from SOUND_DIR
