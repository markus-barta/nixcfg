# Common justfile utilities (stub version)
# This is a minimal stub that works without devenv
# When devenv is active, this may be overlaid by nix-shared/common.just

# This stub provides compatibility when nix-shared is not available
# Add any common utilities here that are used across the justfile

# Placeholder for common utilities from nix-shared
# The full version from github:pbek/nix-shared will be used when devenv is active

